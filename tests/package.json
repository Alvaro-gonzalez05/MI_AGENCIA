{
  "name": "mi-agencia-tests",
  "private": true,
  "type": "module",
  "description": "Verificacion del esquema, del semaforo crediticio y del motor de calculo contra un Postgres real (PGlite/WASM). No necesita Docker ni conexion a Supabase.",
  "scripts": {
    "test": "node generar_sql_completo.mjs --verificar && node migraciones.mjs && node semaforo_bcra.mjs && node paridad_motor.mjs",
    "migraciones": "node migraciones.mjs",
    "semaforo": "node semaforo_bcra.mjs",
    "paridad": "node paridad_motor.mjs",
    "sql-completo": "node generar_sql_completo.mjs"
  },
  "dependencies": {
    "@electric-sql/pglite": "^0.3.11"
  }
}
